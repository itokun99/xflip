import { LoginRequestModel, RegisterRequestModel } from "@core/models/auth";

export const authService = {
  login: async (payload: LoginRequestModel) => {},
  register: async (payload: RegisterRequestModel) => {},
};
